# 实施计划与验收

## 当前完成情况（2026-08-03）

- **阶段 0：完成。** 项目、文档、GitHub 仓库、TypeScript/Vite 构建基线与规则研究已建立。
- **阶段 1：完成。** 正式美术资源、四张核心界面稿、动态 React 牌桌与视觉验收均已完成。
- **阶段 2：共享核心完成。** 权威纯 TypeScript 比赛状态机已抽为 `@wizzard/game-core` 唯一源码包，Intent/Event、查看者快照、AI 同接口和本地适配器已经落地，并跑通完整 8 轮快速局。
- **阶段 3：服务端切片完成。** Node.js/WebSocket 权威房间服务、好友房生命周期、Redis、重连、分享/恢复令牌和服务端计时已经落地。
- **阶段 4：联网底座完成。** 正式 Cocos Creator 3.8.x 工程、语义资源同步、本地/网络适配器与完整动态牌桌已落地；好友房大厅视图、微信能力、部署和真机发布尚未开始。

React/Vite 版本仍是交互与架构验证工具。Cocos 默认开发配置仍运行本地适配器，但已具备显式网络配置与 `NetworkMatchAdapter`；服务端是本地可运行实现，尚未部署上线。当前机器没有安装 Creator，因此编辑器预览和微信构建尚未执行。

## 本轮已实现

### 权威比赛状态机

- 新增 `AuthoritativeMatchState`，集中持有轮次、发牌者、手牌、王牌、叫墩、当前一墩、分数和版本；
- 新增 `MatchIntent`、`MatchEvent` 与 `MatchTransition`；
- 实现命令 ID 首次结果缓存/重放、状态版本检查、阶段检查、行动者检查与稳定错误码；
- `MatchIntent` 不再信任客户端自报玩家 ID，行动者由适配器会话注入；
- 实现翻牌后固定王牌、无王牌或发牌者选择王牌；
- 实现全员叫墩、合法牌集合、完整一墩出牌与胜者判定；
- 实现一墩结束后的下一领出者、整轮结束后的计分与累计分；
- 实现下一轮发牌者顺时针轮转；
- 实现完整 8 轮快速局和比赛结束总榜。

### 查看者快照与 AI

- `createPlayerSnapshot` 从公共快照移除全部手牌、已处理命令 ID 与命令结果缓存；
- 公共状态只保留各座位手牌数量；
- 私有状态只包含当前查看者手牌和当前合法牌 ID；
- 快照内卡牌、玩家、墩和计分条目与权威状态隔离复制；
- AI 使用与真人相同的查看者快照、意图和状态机入口，不获得额外私有信息；
- 当前启发式 AI 可以选择王牌、叫墩并完成合法出牌。

### 本地适配器与动态牌桌

- `useLocalMatch` 作为临时本地适配器，在同一浏览器进程中驱动真人和 AI；
- React controller 只暴露查看者快照，不再暴露完整 `AuthoritativeMatchState`；
- 带随机源的跨轮转换在 reducer 外计算，避免 Strict Mode/并发重放重复消耗 RNG；
- 选择王牌、叫墩与出牌均显示 30 秒倒计时，超时通过同一 Intent 接口托管本次行动；
- 动态牌桌根据快照渲染六席、发牌者、AI 标识、手牌数和角色表情；
- 已实现动态叫墩面板、王牌选择面板、合法/非法牌、手牌选择与确认出牌；
- 已实现当前一墩、赢墩反馈、羊皮卷轮结算、继续下一轮和快速局总榜；
- 已实现 3–6 人查看者相对座位映射，以及 `844×390` 短横屏不溢出的布局；
- 当前牌桌由可拆分组件和正式美术资源组成，可迁移为 Cocos Scene/Prefab，而不是依赖整屏截图热点。

### 共享包与 Cocos Creator 首切片

- 将原 `src/game/` 规则源码迁入 `packages/game-core`，Web、Cocos 与未来 Node 服务不再复制规则；
- 新增 `contracts` 与 `authority` 分离导出，正式网络客户端只需要消费契约；
- 初始化 `cocos-client/` Cocos Creator 3.8.x 2D 工程、横版 `1920×1080` 场景和微信小游戏横屏模板；
- 新增 `IMatchAdapter` 与 `LocalMatchAdapter`，表现层不再接触 `commandId`、版本或行动者注入；
- 新增正式 `MatchSceneView`、`CardView`、`PlayerSeatView` 与 UI 工厂，完成王牌、预测、合法牌、确认出牌、一墩、轮结算和总榜；
- 新增 `tools/sync-cocos-assets.mjs`，从美术清单同步首切片 31 张 PNG，并生成稳定的 Cocos 语义地址；
- 补齐两套精简 Noto SC 静态字体、许可证和来源说明，避免直接打包 40 MiB 变量字体；
- 新增 Cocos 适配器集成验证，确认同一私有快照边界可完成完整 8 轮快速局。

## 阶段 0：项目基线 — 已完成

- 固化规则、视觉方向、技术边界与资料来源；
- 建立 TypeScript、构建与 GitHub 仓库；
- 保存原始规则照片和选定视觉稿。

## 阶段 1：美术资源与交互验证 — 已完成

- 完成 1024×1536 卡牌源文件与 256×384 运行时牌；
- 完成六角色四表情的 1024×1024 源文件与 512×512 运行时头像；
- 完成规则提示插图和三组四帧透明教程手势；
- 完成首页、好友房、动态牌桌、轮结算和视觉对比验收；
- 完成卡牌局部返修与头像裁切分隔线清理。

## 阶段 2：平台无关比赛核心 — 进程内完成

- 牌组、发牌、模式轮数、王牌、叫墩、合法牌、赢墩和计分纯函数已完成；
- 权威比赛状态机与跨墩、跨轮、比赛结束推进已完成；
- Intent/Event、版本控制、幂等命令和查看者快照已完成；
- 适配器注入行动者、成功响应幂等重放、显式权威推进类型和 30 秒本地托管已完成；
- AI 同接口和本地适配器已完成；
- React 动态牌桌已接入真实状态机，不再使用伪造页面状态确认赢墩或计分。

本阶段的“完成”指平台无关核心和本地验证闭环完成，不包含远程房间服务、持久化或生产运维。

## 阶段 3：权威好友房服务 — 服务端切片完成

已完成内容：

1. 抽象本地/网络统一 `MatchAdapter` 接口；
2. 建立 Node.js WebSocket 房间服务，并在服务端调用现有 `@wizzard/game-core/authority`；
3. 实现创建房间、加入、准备、房主开始、AI 补位和房间关闭；
4. 为每个连接生成查看者快照，按玩家过滤私有手牌；
5. 将现有 commandId 首次结果重放与 expectedVersion 冲突恢复接入房间持久化，并保证事件和查看者快照同响应发送；
6. 接入 Redis 活跃房间、短时断线恢复与房间过期；
7. 将当前本地 30 秒托管迁移为服务端计时器，并实现分享令牌、房间码、收回托管与房主退出策略。

完成条件：两台浏览器通过真实 WebSocket 完成一轮，任一客户端抓包均看不到其他玩家手牌或牌库顺序。

## 阶段 4：Cocos Creator 3.8 微信体验版 — 离线首切片完成

已完成：

1. 初始化横版 Cocos Creator 3.8.x 微信小游戏工程骨架；
2. 按 `art/asset-manifest.json` 生成语义资源键、九宫格参数和首切片资源副本；
3. 建立座位、手牌、一墩、状态条、选择面板和结算的模块化 Cocos 视图边界；
4. 实现 Cocos `IMatchAdapter` 与本地权威实现，只向表现层发布事件/查看者快照；
5. 迁移预测、王牌、合法牌、完整一墩、轮结算和 8 轮快速局表现；
6. 设置微信小游戏横屏构建模板并补充 Creator 打开/命令行构建文档。

待完成：

1. 安装 Cocos Creator 3.8.x，完成首次资源导入、浏览器预览与微信小游戏构建；
2. 在编辑器中将稳定视图节点保存为 Prefab，并配置 Auto Atlas、ASTC/ETC2 与小游戏分包；
3. 将已实现的 `NetworkMatchAdapter` 接入好友房大厅视图；
4. 接入微信登录、分享、合法域名与腾讯云托管；
5. 添加体验成员并完成 iPhone/Android 真机检查。

完成条件：好友通过微信分享进入同一房间并完成 8 轮快速局，客户端不能自行确认任何权威规则结果。

## 当前优先级

1. 安装 Creator 并完成 Cocos 首次预览/微信构建验证；
2. Cocos 好友房大厅创建/加入/准备/开始视图；
3. 微信登录、分享入口和真机好友房闭环；
4. Asset Bundle/分包、真机性能与交互精修；
5. 自动化测试扩充与非阻断 QA。

测试相关任务按当前要求置于最低优先级；仅在出现阻断实现的问题时先补必要的最小验证，不以扩充测试数量替代功能实现。

## 验收矩阵

### 当前进程内闭环

- 规则：叫墩、王牌、合法牌、一墩胜者、轮分和总分由同一权威状态机产生；
- 隐私：React controller、人类与 AI 都通过查看者快照读取状态，不直接消费完整手牌表；
- 流程：六席可完成叫墩、整轮出牌、轮结算、发牌者轮转和 8 轮快速局；
- UI：动态牌桌能表现当前行动者、30 秒倒计时、合法牌、一墩、轮结算和总榜，并通过桌面/短横屏 Design QA；
- 边界：React 只作为验证工具，不承担正式联网权威。

### 当前 Cocos 离线闭环

- 工程：`cocos-client/` 可被 Cocos Creator 3.8.x 打开，设计分辨率与微信模板均锁定横版；
- 规则：Cocos 通过 `@wizzard/game-core` 构建产物复用唯一状态机，不存在第二份规则源码；
- 边界：Scene 只调用 `IMatchAdapter`，Node 不接触权威状态；
- 资源：31 张首切片正式 PNG 与 2 套精简字体由语义清单生成并完成路径校验；
- 流程：Cocos 本地适配器已通过完整 8 轮集成验证；
- 静态验证：Creator 3.8 官方类型声明下 TypeScript 检查通过；
- 待验证：由于本机未安装 Creator，真实 SpriteFrame 导入、Scene 预览和微信构建尚未验收。

### 正式体验版待验收

- 网络：重复、乱序和陈旧意图不会造成分叉；
- 隐私：任何网络事件或恢复快照都不暴露其他手牌或牌库顺序；
- 弱网：普通网络重连后目标 3 秒内恢复；
- 体验：快速局约 12–18 分钟，核心行动无需打开二级页面；
- 设备：至少两台 iPhone、两台 Android，窄屏与长屏均不遮挡核心按钮；
- 性能：目标设备保持不低于 30 FPS；
- 视觉：最终 Cocos Design QA 无 P0/P1/P2 问题。
