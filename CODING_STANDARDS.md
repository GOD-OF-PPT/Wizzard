# Coding Standards

- 业务与规则代码使用 TypeScript 严格模式，不使用隐式 `any`。
- 规则引擎必须是纯逻辑模块，不直接依赖 React、微信 API、Canvas 或网络层。
- 服务端是多人对局的唯一权威来源；客户端只提交意图，不自行确认出牌、赢墩或计分结果。
- 公共接口使用项目领域词汇：`round`（轮）、`trick`（墩）、`bid`（预测）、`trump`（王牌）、`leadSuit`（领出花色）。
- 测试通过公共接口验证行为，不测试私有实现细节。
- 所有玩家可见状态与私有手牌状态必须使用不同类型，避免隐藏信息泄漏。
- 平台能力通过适配器注入，不允许在规则模块中直接调用 `wx.*`。
- 美术资源使用稳定语义键，不将原始文件名作为公共 API。
- 提交前至少运行 `npm run typecheck` 与 `npm run build`；规则模块建立后还需运行完整测试套件。
